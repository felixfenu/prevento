const controller = {
    listado: (req,res)=>{
        res.render('products/carrocompras.ejs');
    },
    comprar: (req,res)=>{
        //codigo para comprar entrada.
    }
};

module.exports = controller;

